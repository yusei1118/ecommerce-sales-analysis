
-----Views and visitors detail-----------------------

SELECT 
  COUNT(DISTINCT user_id) AS total_users,
  COUNT(DISTINCT CASE WHEN user_type = 'New' THEN user_id END) AS new_users,
  COUNT(DISTINCT CASE WHEN user_type = 'Returning' THEN user_id END) AS returning_users,

  ROUND(
    COUNT(DISTINCT CASE WHEN user_type = 'New' THEN user_id END) * 100
    / COUNT(DISTINCT user_id), 2) AS new_user_percentage,

  ROUND(
    COUNT(DISTINCT CASE WHEN user_type = 'Returning' THEN user_id END) * 100
    / COUNT(DISTINCT user_id), 2) AS returning_user_percentage,

  ROUND(SUM(CASE WHEN user_type = 'New' THEN revenue ELSE 0 END),0) AS new_customer_sales,
ROUND(SUM(CASE WHEN user_type = 'Returning' THEN revenue ELSE 0 END),0) AS returning_customer_sales,
ROUND(SUM(CASE WHEN user_type = 'New' THEN revenue ELSE 0 END)*100/SUM(revenue),0) AS new_customer_sales_percentage,
ROUND(SUM(CASE WHEN user_type = 'Returning' THEN revenue ELSE 0 END)*100/SUM(revenue),0) as returning_customer_sales_percentage,

FROM `lunar-carving-457020-h5.clean.marketing`;
------sales by channel----------------------

SELECT 
ROUND(SUM(CASE WHEN channel='Email' then revenue else 0 end),0) as email_sales,
ROUND(SUM(CASE WHEN channel='Email' then revenue else 0 end)/SUM(revenue)*100,0) as email_sales_percentage,
ROUND(SUM(CASE WHEN channel='Organic' then revenue else 0 end),0) as organic_sales,
ROUND(SUM(CASE WHEN channel='Organic' then revenue else 0 end)/SUM(revenue)*100,0) as organic_sales_percentage,
SUM(CASE WHEN channel='Paid Ads' then revenue else 0 end) as paidAds_sales,
Round(SUM(CASE WHEN channel='Paid Ads' then revenue else 0 end)/SUM(revenue)*100,0) as paidAds_sales_percentage,
ROUND(SUM(CASE WHEN channel='Social' then revenue else 0 end),0) as social_sales,
ROUND(SUM(CASE WHEN channel='Social' then revenue else 0 end)/SUM(revenue)*100,0) as social_sales_percentage,
FROM `lunar-carving-457020-h5.clean.marketing`;



-----------------------
SELECT
SUM(CASE WHEN channel='Paid Ads' AND user_type='New' then user_id else 0 end) as paidAds_new_sales,
SUM(CASE WHEN channel='Paid Ads' AND user_type='Returning' then user_id else 0 end) as paidAds_returning,
SUM(CASE WHEN channel='Organic' AND user_type='New' then user_id else 0 end) as organic_new_sales,
SUM(CASE WHEN channel='Organic' AND user_type='Returning' then user_id else 0 end) as organic_returning,
SUM(CASE WHEN channel='Paid Ads' AND user_type='New' then revenue else 0 end) as paidAds_new_sales,
SUM(CASE WHEN channel='Paid Ads' AND user_type='Returning' then revenue else 0 end) as paidAds_returning,
SUM(CASE WHEN channel='Organic' AND user_type='New' then revenue else 0 end) as organic_new_sales,
SUM(CASE WHEN channel='Organic' AND user_type='Returning' then revenue else 0 end) as organic_returning,


FROM `lunar-carving-457020-h5.clean.marketing`;

-----Repeaters---

WITH user_summary AS (
  SELECT
    user_id,
    user_type,
    COUNTIF(purchase_completed = true) AS purchase_count
  FROM `lunar-carving-457020-h5.clean.marketing`
  GROUP BY user_id, user_type
)

SELECT
  user_type,
  COUNT(*) AS total_users,
  COUNT(CASE WHEN purchase_count >= 2 THEN 1 END) AS repeat_users,
  ROUND(
    COUNT(CASE WHEN purchase_count >= 2 THEN 1 END) * 100.0 / COUNT(*),
    2
  ) AS repeat_rate
FROM user_summary
GROUP BY user_type;

--------Statistics of buying---------------
WITH stages as (
SELECT
SUM(CASE WHEN visited_website=true then 1 else 0 end) as total_visitors,
SUM(CASE WHEN viewed_product=true then 1 else 0 end) as total_viewers,
SUM(CASE WHEN added_to_cart=true then 1 else 0 end) as total_cart,
SUM(CASE WHEN checkout_started=true then 1 else 0 end) as total_checkout,
SUM(CASE WHEN purchase_completed=true then 1 else 0 end) as total_purchase,

 FROM `lunar-carving-457020-h5.clean.marketing` )

 SELECT total_visitors,
total_viewers,
 ROUND(total_viewers *100/total_visitors,0) as visitors_to_viewers,
 total_cart,
 ROUND(total_cart *100/total_viewers,0) as viewers_to_cart,
 total_checkout,
 ROUND(total_checkout *100/total_cart,0) as cart_to_checkout,
 total_purchase,
 ROUND(total_purchase *100/total_checkout,0) as checkout_to_purchase,
 ROUND(total_purchase *100/total_visitors,0) as visitors_to_purchase,
 ROUND(total_purchase *100/total_viewers,0) as viewrs_to_purchase,
 ROUND(total_purchase *100/total_cart,0) as cart_to_purchase
 FROM stages;


-----------revenue------

WITH b as (
  SELECT
    COUNT( CASE WHEN visited_website = true THEN user_id END) AS total_visitors,
    COUNT( CASE WHEN purchase_completed = true THEN user_id END) AS total_buyers,
    ROUND(SUM(CASE WHEN purchase_completed = true THEN revenue END),2) AS total_revenue

FROM `lunar-carving-457020-h5.clean.marketing`
  )

SELECT 
  total_visitors,
  total_buyers,
  total_revenue,
  ROUND(total_revenue / NULLIF(total_visitors, 0), 2) AS revenue_per_visitor,
  ROUND(total_revenue / NULLIF(total_buyers, 0), 2) AS revenue_per_buyer,


FROM b;


-------royal customer----------

SELECT
  user_id,
  SUM(revenue) AS lifetime_value,

  COUNT(*) AS purchase_times
FROM `lunar-carving-457020-h5.clean.marketing`
WHERE purchase_completed= true
GROUP BY user_id
ORDER BY lifetime_value desc
